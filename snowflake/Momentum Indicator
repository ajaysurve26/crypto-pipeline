SELECT 
  SYMBOL,
  WINDOW_START,
  CASE 
    WHEN PREDICTED_NEXT_CLOSE > TARGET_NEXT_CLOSE THEN 1
    WHEN PREDICTED_NEXT_CLOSE < TARGET_NEXT_CLOSE THEN -1
    ELSE 0
  END AS MOMENTUM_SIGNAL
FROM VW_PREDICTIONS;
