SELECT
    SYMBOL,
    ROUND(
        100 - AVG(ABS(TARGET_NEXT_CLOSE - PREDICTED_NEXT_CLOSE) / TARGET_NEXT_CLOSE * 100),
        2
    ) AS MODEL_ACCURACY
FROM PREDICTIONS_NEXT_MINUTE
GROUP BY SYMBOL
ORDER BY SYMBOL;
